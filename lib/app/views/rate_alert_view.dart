import 'package:flutter/material.dart';

class RateAlertView extends StatelessWidget {
  const RateAlertView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Rate alert'),
      ),
    );
  }
}