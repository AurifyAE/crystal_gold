import 'package:flutter/material.dart';

class SpotRateView extends StatelessWidget {
  const SpotRateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Spot rate'),
      ),
    );
  }
}